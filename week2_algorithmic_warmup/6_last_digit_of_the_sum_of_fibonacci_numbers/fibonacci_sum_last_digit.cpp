#include <iostream>
#include <vector>
typedef long long ll;
using namespace std;


ll get_period_length(ll n, ll m)
{
    ll a[n + 2];
    a[0] = 0;
    a[1] = 1;
    for (int i = 2; i < n; i++)
    {
        
    }
    
}


int main() {
    long long n = 0;
    std::cin >> n;
    // std::cout << fibonacci_sum_naive(n);
}
